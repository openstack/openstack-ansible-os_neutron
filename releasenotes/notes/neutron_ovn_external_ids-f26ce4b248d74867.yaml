---

features:
  - |
    Introduced two new variables ``neutron_ovn_controller_ovs_external_ids``
    and ``neutron_ovn_gateway_ovs_external_ids`` which allow operator to
    control and override default values for ``external_ids`` which will be
    configured during OVN installation.
